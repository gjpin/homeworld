# https://help.ente.io/self-hosting/installation/config
# https://github.com/ente-io/ente/blob/main/server/config/example.yaml
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ente-config
  namespace: ente
  annotations:
    argocd.argoproj.io/sync-wave: "2"
data:
  db:
    host: # TODO postgres
    port: # TODO 5432
    name: # TODO ente_db
    user: # TODO pguser
    password: # TODO <password>
  apps:
    accounts: https://2fa.${BASE_DOMAIN}
  key:
    encryption: ${ENTE_KEY_ENCRYPTION}
    hash: ${ENTE_KEY_HASH}
  jwt:
    secret: ${ENTE_JWT_SECRET}
---